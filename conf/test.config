/*
 * -------------------------------------------------
 *  Nextflow config file for running tests
 * -------------------------------------------------
 * Defines bundled input files and everything required
 * to run a fast and simple test. Use as follows:
 *   nextflow run nf-core/methylseq -profile test
 */
// https://www.ebi.ac.uk/ena/data/view/SRX3242046

executor {
  name = 'local'
}

params {
  max_cpus = 2
  max_memory = 6.GB
  max_time = 48.h
  // Input data
  singleEnd = false
  fusioncatcher_dir = '/data/fc'
  star_fusion_ref  = '/data/sf/ctat_genome_lib_build_dir'
}
